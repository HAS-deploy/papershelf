import Foundation

/// Local SoT display prices — must match Configuration.storekit and ASC when created.
enum PricingConfig {
    static let yearlyDisplayPrice = "$19.99"
    static let lifetimeDisplayPrice = "$39.99"
    static let yearlyPeriodLabel = "per year"
    static let lifetimePeriodLabel = "one-time"
}
